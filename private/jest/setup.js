/**
* Setup file that runs before every test.
*
* Used to make global variables available to all Jest tests.
*/

/**
* Global variables
*/
// global.$ = require('jquery');

// Override to suppress library deprecation warnings, comment out to see warnings
console.warn = () => {};
